<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', { title: event.title, pageType: 'events' }) %>
<body>
    <%- include('../partials/header') %>
    <%- include('../partials/nav', { currentPage: 'events' }) %>

    <section class="event-details">
        <section class="event-header">
            <h1><%= event.title %></h1>
            <p><%= event.details %></p>
        </section>
        
        <h2>Event Details</h2>
        <ul>
            <li><strong>Category:</strong> <%= event.category %></li>
            <li><strong>Host:</strong> <%= event.host %></li>
            <li><strong>Start Date & Time:</strong> <%= new Date(event.startDateTime).toLocaleString() %></li>
            <li><strong>End Date & Time:</strong> <%= new Date(event.endDateTime).toLocaleString() %></li>
        </ul>
        
        <div class="event-actions">
            <a href="/events/<%= event.id %>/edit" class="edit-event-button">Edit Event</a>
            <form method="POST" action="/events/<%= event.id %>?_method=DELETE" style="display: inline;">
                <button type="submit" class="delete-event-button" onclick="return confirm('Are you sure you want to delete this event?')">Delete Event</button>
            </form>
        </div>
    </section>

    <%- include('../partials/footer') %>
</body>
</html>